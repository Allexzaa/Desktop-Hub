{
  "name": "desktop-hub",
  "version": "1.0.0",
  "description": "A Windows desktop hub application with tabbed functionality",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run start-react\" \"npm run start-backend\" \"wait-on http://localhost:3010 http://localhost:8082 && cross-env ELECTRON_DEV=true electron .\" --names \"REACT,BACKEND,ELECTRON\" --prefix-colors \"blue,green,yellow\"",
    "dev-build": "npm run build && concurrently \"npm run start-backend\" \"electron .\" --names \"BACKEND,ELECTRON\" --prefix-colors \"green,yellow\"",
    "start-react": "craco start",
    "start-backend": "node src/services/youtube-ai-summarizer/backend/server.js",
    "build": "craco build",
    "build-electron": "npm run build && electron-builder",
    "dist": "npm run build && electron-builder --publish=never"
  },
  "author": "Desktop Hub",
  "license": "MIT",
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "concurrently": "^7.6.0",
    "cross-env": "^7.0.3",
    "electron": "^38.1.2",
    "electron-builder": "^24.6.4",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "homepage": "./",
  "build": {
    "appId": "com.desktophub.app",
    "productName": "Desktop Hub",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "src/main.js",
      "node_modules/**/*"
    ],
    "win": {
      "target": "nsis"
    }
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}